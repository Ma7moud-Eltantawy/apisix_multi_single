# ============================================================
#  Nginx — Unified Control Plane & Admin Proxy (Port 9012)
# ============================================================

upstream dashboard_backend {
    server apisix-dashboard:9000;
    keepalive 32;
}

upstream admin_api_backend {
    server apisix:9180;
    keepalive 32;
}

server {
    listen 9180;
    server_name _;

    # ── 1. APISIX Dashboard Manager API (Admin REST API & Auth) ────────────
    location /apisix/admin/ {
        proxy_pass         http://dashboard_backend/apisix/admin/;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   Connection        "";
        proxy_read_timeout 60s;
        client_max_body_size 10m;
    }

    # ── 2. Official Apache APISIX Dashboard UI (All Web Routes) ────────────
    location / {
        proxy_pass         http://dashboard_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   Connection        "";
        proxy_read_timeout 60s;
    }
}
